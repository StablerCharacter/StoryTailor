import 'package:flame_character/flame_character.dart';

class GameConfig {
  /// The asset loader global instance. Can be changed to
  /// allow alternative implementations.
  static AssetManager assetManager = AssetManager();
}
